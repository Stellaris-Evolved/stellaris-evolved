# paragon/tec_leader_designation.txt

inline_script = {
	script = tec_type_hint
	designation_type = $designation$
	leader_trigger_block = "$trigger$"
}

modifier = {
	exists = FROM
	FROM = {
		is_scope_type = planet
		has_$designation$_designation = yes
	}

	$trigger$

	mult = value:scripted_modifier_mult|MODIFIER|tec_leader_$designation$_designation_weight|
}