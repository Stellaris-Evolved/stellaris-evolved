# ethics/tec_ethic_federation_value.txt

inline_script = {
    script = tec_type_hint
    federation_type = $type$
    number_of_value = $mult$
}

modifier = {
    mult = $mult$
    has_federation = yes
    federation = {
        has_federation_type = $type$
    }
}