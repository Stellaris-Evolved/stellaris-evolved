# ethics/tec_ethic_pop_xeno_slaves_value.txt

inline_script = {
    script = tec_type_hint
    number_or_value = $mult$
}

modifier = {
    mult = $mult$
    from = {
        any_enslaved_species = {
            NOT = { is_same_species = root }
            is_robotic = no
        }
    }
}