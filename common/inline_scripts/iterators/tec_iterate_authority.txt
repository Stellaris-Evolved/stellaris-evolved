# iterators/tec_iterate_authority.txt

inline_script = {
	script = iterators/_tec_iterate_authority
	code = "$code$"
	authority = auth_democratic
	authority_short = democratic
}

inline_script = {
	script = iterators/_tec_iterate_authority
	code = "$code$"
	authority = auth_oligarchic
	authority_short = oligarchic
}

inline_script = {
	script = iterators/_tec_iterate_authority
	code = "$code$"
	authority = auth_dictatorial
	authority_short = dictatorial
}

inline_script = {
	script = iterators/_tec_iterate_authority
	code = "$code$"
	authority = auth_imperial
	authority_short = imperial
}

inline_script = {
	script = iterators/_tec_iterate_authority
	code = "$code$"
	authority = auth_corporate
	authority_short = corporate
}

inline_script = {
	script = iterators/_tec_iterate_authority
	code = "$code$"
	authority = auth_tec_patrocorporate
	authority_short = tec_patrocorporate
}

inline_script = {
	script = iterators/_tec_iterate_authority
	code = "$code$"
	authority = auth_tec_ai
	authority_short = tec_ai
}

inline_script = {
	script = iterators/_tec_iterate_authority
	code = "$code$"
	authority = auth_tec_ai_corporate
	authority_short = tec_ai_corporate
}

inline_script = {
	script = iterators/_tec_iterate_authority
	code = "$code$"
	authority = auth_hive_mind
	authority_short = hive_mind
}

inline_script = {
	script = iterators/_tec_iterate_authority
	code = "$code$"
	authority = auth_tec_hive_biological
	authority_short = tec_hive_biological
}

inline_script = {
	script = iterators/_tec_iterate_authority
	code = "$code$"
	authority = auth_tec_hive_cybernetic
	authority_short = tec_hive_cybernetic
}

inline_script = {
	script = iterators/_tec_iterate_authority
	code = "$code$"
	authority = auth_machine_intelligence
	authority_short = machine_intelligence
}