# common\inline_scripts\iterators\tec_iterate_leader_class.txt

inline_script = {
	script = iterators/_tec_iterate_leader_class
	code = "$code$"
	leader_class = official
}

inline_script = {
	script = iterators/_tec_iterate_leader_class
	code = "$code$"
	leader_class = commander
}

inline_script = {
	script = iterators/_tec_iterate_leader_class
	code = "$code$"
	leader_class = scientist
}
