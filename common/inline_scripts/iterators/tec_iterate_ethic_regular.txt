# iterators/tec_iterate_ethic_regular.txt

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = materialist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = spiritualist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = militarist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = pacifist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = egalitarian
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = authoritarian
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = xenophobe
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = xenophile
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = ecologist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = industrialist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = competitive
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = cooperative
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = elitist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
    code = "$code$"
	ethic = pluralist
}
