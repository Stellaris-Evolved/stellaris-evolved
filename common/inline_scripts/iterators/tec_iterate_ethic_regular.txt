# iterators/tec_iterate_ethic_regular.txt

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = materialist
	opposing_ethic = spiritualist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = spiritualist
	opposing_ethic = materialist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = militarist
	opposing_ethic = pacifist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = pacifist
	opposing_ethic = militarist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = egalitarian
	opposing_ethic = authoritarian
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = authoritarian
	opposing_ethic = egalitarian
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = xenophobe
	opposing_ethic = xenophile
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = xenophile
	opposing_ethic = xenophobe
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = ecologist
	opposing_ethic = industrialist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = industrialist
	opposing_ethic = ecologist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = competitive
	opposing_ethic = cooperative
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = cooperative
	opposing_ethic = competitive
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = elitist
	opposing_ethic = pluralist
}

inline_script = {
	script = iterators/_tec_iterate_ethic_regular
	code = "$code$"
	ethic = pluralist
	opposing_ethic = elitist
}