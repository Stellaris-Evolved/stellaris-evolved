# iterators/leader_class/tec_refragment_aiauth_ruler.txt

inline_script = {
    script = tec_type_hint
    leader_class = $class$
    ethic = $ethic$
}

if = {
    limit = {
        has_country_flag = tec_fragmenting_$leader_class$
    }
    tec_country_create_aiauth_ruler = {
        class = $leader_class$
        ethic = $ethic$
    }
}