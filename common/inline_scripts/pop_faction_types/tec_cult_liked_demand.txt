inline_script = {
    script = tec_type_hint
    tec_cult = $cult$
    int = $approval$
}

demand = {
    # Ruler = FANTASTIC
    title = faction_demand_TEC_CULT_$cult$
    unfulfilled_title = LABEL_NA
    desc = faction_demand_TEC_CULT_$cult$_liked_desc

    fulfilled_effect = $approval$
    unfulfilled_effect = 0

    potential = {
        exists = owner
        owner = {
            tec_has_cult = {
                cult = $cult$
            }
        }
    }

    trigger = { always = yes }	# Always happy if Potential fulfilled.
}