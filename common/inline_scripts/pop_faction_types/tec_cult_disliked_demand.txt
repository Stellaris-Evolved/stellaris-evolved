inline_script = {
    script = tec_type_hint
    tec_cult = $cult$
    int = $approval$
}

demand = {
    # Ruler = FANTASTIC
    title = LABEL_NA
    unfulfilled_title = faction_demand_TEC_CULT_$cult$
    desc = faction_demand_TEC_CULT_$cult$_disliked_desc

    fulfilled_effect = 0
    unfulfilled_effect = $approval$

    potential = {
        exists = owner
        owner = {
            tec_has_cult = {
                cult = $cult$
            }
        }
    }

    trigger = { always = no }	# Always unhappy if Potential fulfilled.
}
