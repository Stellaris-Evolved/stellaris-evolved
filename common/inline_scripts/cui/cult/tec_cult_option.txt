inline_script = {
    script = tec_type_hint
    cult_include_step = $_step$
    # tec_cult = $name$
    country_trigger_block = "$potential$"
    country_trigger_block = "$allow$"
    country_effect_block = "$on_activate$"
    country_effect_block = "$on_deactivate$"
    modifier_block = "$modifier$"
    int = $grid_x$
    int = $grid_y$
}

inline_script = {
    script = cui/cult/tec_cult_option_$_step$
    name = $name$
    grid_x = $grid_x$
    grid_y = $grid_y$
    potential = "$potential$"
    potential = "always = yes"
    allow = "$allow$"
    allow = "always = yes"
    on_activate = "$on_activate$"
    on_activate = ""
    on_deactivate = "$on_deactivate$"
    on_deactivate = ""
    modifier = "$modifier$"
    modifier = ""
}