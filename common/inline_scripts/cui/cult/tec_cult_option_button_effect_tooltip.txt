# cui/cult/tec_cult_option_button_effect_tooltip.txt

inline_script = {
    script = tec_type_hint
    tec_cult = $name$
    country_scope = $from$
    bool = $skip_header$
}

if = {
    limit = {
        NOT = {
            always = $skip_header$
        }
    }
    custom_tooltip = tec_cult_$name$_header
}

if = {
    limit = {
        $from$ = {
            tec_has_cult = {
                cult = $name$
            }
        }
    }
    custom_tooltip = tec_cui_cult_active
}
custom_tooltip = tec_cult_$name$_desc
custom_tooltip = tec_loc_desc_separator
if = {
    limit = {
        $from$ = {
            tec_has_cult = {
                cult = $name$
            }
        }
    }
    custom_tooltip = tec_cui_cult_tooltip_disabled_start
    custom_tooltip = tec_cui_cult_current_effects_section
} else = {
    custom_tooltip = tec_cui_cult_tooltip_normal_start
    custom_tooltip = tec_cui_cult_effects_section
}
tooltip = {
    $from$ = {
        tec_activate_cult = {
            cult = $name$
            force = yes
            tooltip = yes
        }
    }
}
custom_tooltip = tec_cui_cult_tooltip_color_end
if = {
    limit = {
        $from$ = {
            NOT = {
                tec_has_cult = {
                    cult = $name$
                }
            }
        }
    }
    custom_tooltip = tec_cui_cult_tooltip_disabled_start
} else = {
    custom_tooltip = tec_cui_cult_tooltip_normal_start
}
custom_tooltip = tec_cui_cult_on_deactivate_effects_section
tooltip = {
    $from$ = {
        tec_deactivate_cult = {
            cult = $name$
            tooltip = yes
        }
    }
}
custom_tooltip = tec_cui_cult_tooltip_color_end

$from$ = {
    if = {
        limit = {
            NOT = {
                tec_has_cult = {
                    cult = $name$
                }
            }
        }
        hidden_effect = {
            tec_internal_cult_activate_confirmation = {
                cult = $name$
            }
        }
    } else = {
        hidden_effect = {
            tec_internal_cult_deactivate_confirmation = {
                cult = $name$
            }
        }
    }
}