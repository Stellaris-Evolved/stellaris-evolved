# cui/cult/tec_cult_option_clear_cults.txt

if = {
    limit = {
        tec_has_cult = {
            cult = $name$
        }
    }

    tec_deactivate_cult = { cult = $name$ }
    remove_country_flag = tec_cult_$name$_last_deactivated
}