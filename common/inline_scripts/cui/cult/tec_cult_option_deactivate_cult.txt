# cui/cult/tec_cult_option_deactivate_cult.txt

if = {
    limit = {
        tec_has_cult = {
            cult = $name$
        }
    }

    remove_country_flag = tec_cult_$name$
    remove_modifier = tec_cult_$name$_modifier
    tec_clear_cult_deactivate_flag = yes
    set_country_flag = tec_cult_$name$_last_deactivated
    $on_deactivate$
    fire_on_action = {
        on_action = tec_cult_on_deactivate
    }
    hidden_effect = {
        change_variable = {
            which = tec_cult_points_used
            value = -1
        }
    }
}