# capitals/inline_evolved_special_capital
# $tier$ adding here for script to not scream

# Capital Jobs
	# Regular
		inline_script = {
			script = jobs/inline_evolved_politician
			trigger = ""
			count = $count$
			mult = 1
			display = with_desc
		}
	# /
	# Worker coops
		inline_script = { # Steward
			script = jobs/inline_evolved_job
			trigger = "
				exists = owner

				$trigger$
				
				owner = {
					tec_is_worker_coop_empire = yes
				}
			"
			job = steward
			count = @tec_capital_add_steward_jobs_tspecial$tier$
			mult = $mult$
			display = $display$
		}
	# /
# /

inline_script = capitals/effects/inline_evolved_councilor_feudal_realm

triggered_desc = {
	trigger = {
		always = no
		tec_always_from_param = { PARAM = $tier$ }
	}
	text = tec_blank
}