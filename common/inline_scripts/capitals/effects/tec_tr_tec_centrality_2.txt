# capitals/effects/tec_tr_tec_centrality_2.txt
# Politicians
# full
inline_script = {
	script = jobs/tec_politician
	trigger = "
		exists = owner
		owner = {
			has_active_tradition = tr_tec_centrality_2
		}
		NOT = { inline_script = { script = tec_read_cache scope = planet trigger = tec_has_special_ruler_feature } }

		$ruler_trigger$

		owner = {
			NOT = { inline_script = { script = tec_read_cache scope = country trigger = tec_has_ruler_civic } }
		}
	"
	display = no_desc
	count = @tec_capital_centrality_base_ruler_jobs_t$tier$
	mult = 1
}

# half
inline_script = {
	script = jobs/tec_politician
	trigger = "
		exists = owner
		owner = {
			has_active_tradition = tr_tec_centrality_2
		}
		NOT = { inline_script = { script = tec_read_cache scope = planet trigger = tec_has_special_ruler_feature } }

		$ruler_trigger$

		owner = {
			inline_script = { script = tec_read_cache scope = country trigger = tec_has_ruler_civic }
		}
	"
	display = no_desc
	count = @tec_capital_centrality_base_swap_ruler_jobs_t$tier$
	mult = 1
}
# /

# Politician Swaps
inline_script = {
	script = jobs/tec_politician_civic_swaps
	trigger = "
		exists = owner
		owner = {
			has_active_tradition = tr_tec_centrality_2
		}
		NOT = { inline_script = { script = tec_read_cache scope = planet trigger = tec_has_special_ruler_feature } }

		owner = {
			inline_script = { script = tec_read_cache scope = country trigger = tec_has_ruler_civic }
		}

		$ruler_trigger$
	"
	display = no_desc
	count = @tec_capital_centrality_swap_ruler_jobs_t$tier$
	mult = 1
}
# /

# Megacorp Ruler Civics
inline_script = {
	script = jobs/tec_executive_civic_swaps
	trigger = "
		exists = owner
		owner = {
			has_active_tradition = tr_tec_centrality_2
		}
		NOT = { tec_has_special_ruler_feature = { cached = yes } }

		owner = {
			inline_script = { script = tec_read_cache scope = country trigger = tec_has_ruler_civic }
		}

		$ruler_trigger$
	"
	display = no_desc
	count = @tec_capital_centrality_swap_ruler_jobs_t$tier$
	mult = 1
}
# /