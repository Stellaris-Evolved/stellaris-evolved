# capitals/effects/tec_tr_cybernetics_augmentation_overload_hive

inline_script = { # Statecraft - Admin Drone - uses 1 per (tier - 1) / 2 jobs formula
	script = jobs/tec_job
	trigger = "
		exists = owner
		owner = {
			is_hive_empire = yes
			has_tradition = tr_cybernetics_augmentation_overload
		}
	"
	job = augmentor_drone
	count = @tec_capital_add_ruler_jobs_t$tier$
	mult = 1
	display = with_desc
}