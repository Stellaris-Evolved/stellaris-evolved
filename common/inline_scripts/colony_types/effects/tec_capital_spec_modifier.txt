# colony_types/effects/tec_capital_spec_base.txt

inline_script = {
	script = tec_type_hint
    modifier = $modifier$
}

triggered_planet_modifier = {
    potential = {
        owner = {
            is_country_type = default
            is_machine_empire = no
        }
    }
    $modifier$ = @tec_capital_designation_output_specialized_bonus
}

triggered_planet_modifier = {
    potential = {
        owner = {
            is_country_type = default
            is_machine_empire = yes
        }
    }
    $modifier$ = @[ tec_capital_designation_output_specialized_bonus + tec_capital_designation_machine_bonus ]
}