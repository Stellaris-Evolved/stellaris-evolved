# colony_types/effects/tec_adaptability_generator

triggered_planet_modifier = {	# Adaptability
    potential = {
        exists = owner
        owner = { has_tradition = tr_adaptability_finish }
    }
    modifier = {
        planet_technician_produces_mult = @tec_adaptability_menial_bonus
    }
}