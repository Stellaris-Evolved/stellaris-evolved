# colony_types/effects/tec_adaptability_foundry

triggered_planet_modifier = {	# Adaptability
    potential = {
        exists = owner
        owner = { has_tradition = tr_adaptability_finish }
    }
    modifier = {
        planet_metallurgists_produces_mult = @tec_adaptability_advanced_bonus
    }
}