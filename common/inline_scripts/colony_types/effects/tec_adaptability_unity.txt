# colony_types/effects/tec_adaptability_unity

triggered_planet_modifier = {	# Adaptability
    potential = {
        exists = owner
        owner = { has_tradition = tr_adaptability_finish }
    }
    modifier = {
        planet_administrators_produces_mult = @tec_adaptability_advanced_bonus
    }
}