# governments/tags/tec_ethic_federation_tag

inline_script = {
	script = tec_type_hint
	localisation = $tag$
	federation_type = $type$
}


pop_attraction_tag = {
	desc = $tag$
	trigger = {
		has_federation = yes
		federation = {
			has_federation_type = $type$
		}
	}
}