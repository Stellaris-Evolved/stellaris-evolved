# governments/tags/tec_ethic_migration_tag

inline_script = {
	script = tec_type_hint
	ethic_attraction = $attitude$
}


pop_attraction_tag = {
	desc = POP_ATTRACTION_MIGRATION_TREATIES_$attitude$
	trigger = {
		any_relation = {
			is_subject = no
			has_migration_access = root
			NOT = { is_same_species = root }
		}
	}
}