# governments/conditions/tec_secondary_cult_block.txt

inline_script = {
	script = tec_type_hint
    civic_or_origin = $exclude$
}

civics = {
    NOR = {
        text = civic_tooltip_tec_secondary_cult_block
        inline_script = {
            script = governments/conditions/part/tec_civic_value
            civic = civic_tec_regular_occultist
            exclude = $exclude$
        }
        # not a civic
        inline_script = {
            script = governments/conditions/part/tec_civic_value
            civic = civic_tec_force_debug
            exclude = $exclude$
        }
    }
}