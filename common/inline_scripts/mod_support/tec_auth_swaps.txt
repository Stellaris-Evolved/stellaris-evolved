# mod_support/tec_auth_swaps.txt

inline_script = {
	script = tec_type_hint
    authority = $authority$
}

inline_script = {
    script = mod_support/tec_inlines_include
    include_script = mod_support/tec_$authority$_swaps
    parameters = ""
}