# mod_support/tec_acot_present.txt

# using this shortcut to remove need of too much quoting if necessary

inline_script = {
	script = conditional/parts/tec_step
	x = @has_ancient_caches
	code = "$code$"
}