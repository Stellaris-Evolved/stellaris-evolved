# common\inline_scripts\buildings\on_all_capital_buildings.txt

inline_script = {
	script = capitals/tec_all_capital
	tier = 1
}

# cancelling evolved inclusion of those
triggered_planet_modifier = {
	defense_armies_add = @[tier_1_capital_defense_armies * -1]
	planet_max_buildings_add = @[build_slots_t1 * -1]
	planet_max_branch_office_buildings_add = @[branch_slots_t1 * -1]
}
