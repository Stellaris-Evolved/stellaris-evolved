# buildings/effects/tec_cyber_tec_patrocorporate_individualist.txt

inline_script = {	# tec_pleasure_giver - 1 each 2 ascensions
    script = jobs/tec_job
    job = tec_cyber_idol
    count = $tier$
    mult = 1
    display = with_desc
    trigger = "
        exists = owner
        owner = {
            has_authority = auth_tec_patrocorporate
            has_country_flag = cyber_individualist
        }
    "
    default_job = no
}

inline_script = {
    script = jobs/tec_job
    job = tec_cyber_idol
    count = 1
    mult = planet.value:tec_script_add_per_ascension|BASE|0|ASCENSION_BASE|0|ASCENSION_MULT|@tec_per_planet_ascension_mult_very_low|MULT|1|ROUND|yes|
    display = no_desc
    trigger = "
        exists = owner
        owner = {
            has_authority = auth_tec_patrocorporate
            has_country_flag = cyber_individualist
        }
    "
    default_job = no
}

triggered_desc = {
    trigger = {
        exists = owner
        owner = {
            has_authority = auth_tec_patrocorporate
            has_country_flag = cyber_individualist
        }
    }
    text = tec_building_ascension_scaling
}