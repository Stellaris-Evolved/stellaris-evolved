# modifiers/tec_alternative_upkeep

inline_script = {
	script = tec_type_hint
	planet_trigger_block = "$trigger$"
	float = $count$
	bool = $default$
}

inline_script = {
	script = modifiers/_tec_alternative_upkeep
	count = $count$
	trigger = "$trigger$"
	trigger = "always = yes"
	default = $default$
	default = yes
}