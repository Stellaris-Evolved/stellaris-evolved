# modifiers/_tec_crime_trade

inline_script = {
	script = tec_type_hint
	trigger_block = "$trigger$"
	float = $count$
	number_or_value = "$mult$"
	bool = $default$
}
triggered_planet_modifier = {
	potential = {
		OR = {
			AND = {
				is_scope_valid = no
				always = $default$
			}
			planet = {
				exists = owner

				$trigger$
			}
		}
	}
	branch_office_criminal_syndicate_value_add = $count$
	mult = $mult$
}
