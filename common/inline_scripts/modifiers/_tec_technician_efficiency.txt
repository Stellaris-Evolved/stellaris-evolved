# modifiers/_tec_technician_efficiency

inline_script = {
	script = tec_type_hint
	trigger_block = "$trigger$"
	float = $count$
	number_or_value = "$mult$"
	bool = $default$
}

triggered_planet_modifier = {
	potential = {
		OR = {
			AND = {
				is_scope_valid = no
				always = $default$
			}
			AND = {
				exists = owner

				$trigger$

				OR = {
					NOT = { exists = planet }
					planet = {
						tec_has_technician_production_planet_swap = no
					}
				}

				owner = {
					tec_has_technician_production_swap = no
				}
			}
		}
	}
	planet_technician_energy_produces_add = $count$
	mult = $mult$
}

inline_script = {
	script = mod_support/tec_inlines_include
	include_script = modifiers/tec_technician_efficiency
	parameters = "
		count = $count$
		mult = $mult$
		trigger = \"\\\"$trigger$\"\\\"
	"
}