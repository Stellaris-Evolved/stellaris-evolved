# modifiers/_tec_sr_miner_efficiency.txt

inline_script = {
	script = tec_type_hint
	economic_category = $eco_category$
	resource = $resource$
	trigger_block = "$trigger$"
	number_or_value = "$mult$"
	bool = $default$
}

triggered_planet_modifier = {
	potential = {
		OR = {
			AND = {
				is_scope_valid = no
				always = $default$
			}
			AND = {
				exists = owner

				$trigger$

				always = yes
			}
		}
	}
	$eco_category$_$resource$_produces_mult = 0.2
	mult = $mult$
}