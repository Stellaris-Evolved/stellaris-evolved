# modifiers/_tec_research_efficiency.txt

inline_script = {
	script = tec_type_hint
	trigger_block = "$trigger$"
	float = $count$
	number_or_value = "$mult$"
	bool = $default$
	localisation = $key$
}

triggered_planet_modifier = {
	potential = {
		OR = {
			AND = {
				is_scope_valid = no
				always = $default$
			}
			AND = {
				exists = owner

				$trigger$

				always = yes
			}
		}
	}
	planet_researchers_physics_research_produces_add = $count$
	planet_researchers_society_research_produces_add = $count$
	planet_researchers_engineering_research_produces_add = $count$

	mult = $mult$
	key = $key$
}

triggered_planet_modifier = {
	potential = {
		OR = {
			AND = {
				is_scope_valid = no
				always = $default$
			}
			AND = {
				exists = owner

				$trigger$

				always = yes
			}
		}
	}
	planet_researchers_energy_upkeep_add = @[$count$ * 2]

	mult = $mult$
	key = $key$
}


inline_script = {
	script = mod_support/tec_inlines_include
	include_script = modifiers/tec_research_efficiency
	parameters = "
		count = $count$
		mult = $mult$
		trigger = \"\\\"$trigger$\"\\\"
	"
}