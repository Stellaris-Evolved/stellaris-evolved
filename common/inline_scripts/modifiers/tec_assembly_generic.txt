# modifiers/tec_assembly_generic

inline_script = {
	script = tec_type_hint
	trigger_block = "$trigger$"
	number_or_value = "$mult$"
	bool = $default$
}

inline_script = {
	script = modifiers/_tec_assembly_generic
	trigger = "$trigger$"
	mult = $mult$
	default = $default$
	default = yes
}