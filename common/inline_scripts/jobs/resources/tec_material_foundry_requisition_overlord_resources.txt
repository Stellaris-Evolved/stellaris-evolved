# jobs/resources/tec_material_foundry_requisition_overlord_resources

produces = {
	trigger = {
		planet = {
			has_planet_flag = has_material_requisitorium
		}
	}
	alloys = $alloys$
	multiplier = $mult$
}