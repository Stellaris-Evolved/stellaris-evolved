# jobs/helper/tec_planet_not_exists_or_job_trigger.txt

inline_script = {
	script = tec_type_hint
	scripted_trigger = $trigger$
	bool = $negate$
}

inline_script = {
	script = jobs/helper/_tec_planet_not_exists_or_job_trigger
	trigger = $trigger$
	negate = $negate$
	negate = no
}