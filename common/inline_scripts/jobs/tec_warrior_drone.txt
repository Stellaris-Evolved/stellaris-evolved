# jobs/tec_warrior_drone

# Maybe separate later between machines and hives

inline_script = {
	script = jobs/tec_job
	trigger = "
		exists = owner

		$trigger$
		
		owner = {
			is_gestalt = yes
			NOT = { inline_script = { script = tec_read_cache scope = country trigger = tec_has_warrior_drone_swap } }
		}
	"
	job = warrior_drone
	count = $count$
	mult = $mult$
	display = $display$
	default_job = $default_job$
	default_job = no
	default_trigger = "$default_trigger$"
	default_trigger = "always = no"
}

inline_script = {
	script = mod_support/tec_inlines_include
	include_script = jobs/tec_warrior_drone
	parameters = "
		count = $count$
		mult = $mult$
		display = $display$
		trigger = \"\\\"$trigger$\"\\\"
		default_job = $default_job$
		default_job = no
		default_trigger = \"\\\"$default_trigger$\"\\\"
		default_trigger = \"\\\"always = no\"\\\"
	"
}