# jobs/tec_politician

inline_script = { # Politician
	script = jobs/tec_job
	trigger = "
		exists = owner

		$trigger$

		owner = {
			tec_country_has_politician_ruler = { cached = yes }
		}

		
	"
	job = politician
	count = $count$
	mult = $mult$
	display = $display$
}

# Ai auth
	inline_script = { # Computator
		script = jobs/tec_job
		trigger = "
			exists = owner

			$trigger$

			owner = {
				tec_country_has_tec_ai_computator_ruler = { cached = yes }
			}
		"
		job = tec_ai_computator
		count = $count$
		mult = $mult$
		display = $display$
	}

	inline_script = { # High Ordinator
		script = jobs/tec_job
		trigger = "
			exists = owner

			$trigger$

			owner = {
				tec_country_has_tec_ai_high_ordinator_ruler = { cached = yes }
			}
		"
		job = tec_ai_high_ordinator
		count = $count$
		mult = $mult$
		display = $display$
	}
# /

inline_script = { # Executive
	script = jobs/tec_job
	trigger = "
		exists = owner
		
		$trigger$

		owner = {
			tec_country_has_executive_ruler = { cached = yes }
		}
	"
	job = executive
	count = $count$
	mult = $mult$
	display = $display$
}


inline_script = { # Steward
	script = jobs/tec_job
	trigger = "
		exists = owner

		$trigger$

		owner = {
			tec_country_has_steward_ruler = { cached = yes }
		}
	"
	job = steward
	count = @[$count$ * 2]
	mult = $mult$
	display = $display$
}
