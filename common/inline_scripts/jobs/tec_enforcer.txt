inline_script = { # Enforcers
	script = jobs/tec_job
	trigger = "
		exists = owner

		$trigger$
		
		owner = {
			is_regular_empire = yes
			tec_has_enforcer_swap = no
		}
	"
	job = enforcer
	count = $count$
	display = $display$
}
