# jobs/tec_coordinator

inline_script = {
	script = tec_type_hint
	job_planet_trigger_block = "$trigger$"
}

inline_script = {
	script = jobs/tec_job
	trigger = "
		exists = owner

		$trigger$ 

		owner = {
			is_machine_empire = yes
			NOT = { inline_script = { script = tec_read_cache scope = country trigger = tec_has_coordinator_swap } }
		}
	"
	job = coordinator
	count = $count$
	mult = $mult$
	display = $display$
	default_job = $default_job$
	default_job = no
}

inline_script = {
	script = mod_support/tec_inlines_include
	include_script = jobs/tec_coordinator
	parameters = "
		count = $count$
		mult = $mult$
		display = $display$
		trigger = \"\\\"$trigger$\"\\\"
	"
}