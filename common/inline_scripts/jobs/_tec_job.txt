# jobs/tec_job

# Inline for jobs, dispatches the display type, more useful when conditionals 
# and defaults will be allowed

inline_script = {
    script = conditional/parts/tec_step_disabled_$disabled$
    code = "
        inline_script = {
			script = jobs/tec_job_$display$
			job = $job$
			count = $count$
			mult = $mult$
			default_job = $default_job$
			default_trigger = \"$default_trigger$\"
			trigger = \"$trigger$\"
		}
    "
}