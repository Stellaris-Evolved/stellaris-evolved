# common\inline_scripts\jobs\tec_tec_geneticist_researcher.txt

# Regular
	inline_script = {	# Regular
		script = jobs/tec_job
		trigger = "
			exists = owner

			$trigger$

			owner = {
				is_gestalt = no
			}
		"
		job = tec_geneticist
		count = $count$
		mult = $mult$
		display = $display$
	}
# /

# Gestalt
	inline_script = {	# Gestalt
		script = jobs/tec_job
		trigger = "
			exists = owner

			$trigger$

			owner = {
				is_gestalt = yes
			}
		"
		job = tec_geneticist_drone
		count = $count$
		mult = $mult$
		display = $display$
	}
# /