# jobs/tec_astral_researcher.txt

# Regular
	inline_script = {	# Regular
		script = jobs/tec_job
		trigger = "
			exists = owner

			$trigger$

			owner = {
				inline_script = { script = tec_read_cache scope = country trigger = tec_uses_regular_empire_jobs }
			}
		"
		job = astral_researcher
		count = $count$
		mult = $mult$
		display = $display$
		default_job = $default_job$
		default_job = no
		default_trigger = "$default_trigger$"
		default_trigger = "always = no"
	}
# /

# Gestalt
	inline_script = {	# Gestalt
		script = jobs/tec_job
		trigger = "
			exists = owner

			$trigger$

			owner = {
				is_hive_empire = yes
			}
		"
		job = astral_drone
		count = $count$
		mult = $mult$
		display = $display$
		default_job = $default_job$
		default_job = no
		default_trigger = "$default_trigger$"
		default_trigger = "always = no"
	}
# /

	inline_script = {	# Gestalt
		script = jobs/tec_job
		trigger = "
			exists = owner

			$trigger$

			owner = {
				is_machine_empire = yes
			}
		"
		job = astral_unit
		count = $count$
		mult = $mult$
		display = $display$
		default_job = $default_job$
		default_job = no
		default_trigger = "$default_trigger$"
		default_trigger = "always = no"
	}