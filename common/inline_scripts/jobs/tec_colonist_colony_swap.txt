# tec_colonist_colony_swap

inline_script = {
	script = jobs/tec_enforcer
	count = $count$
	mult = $mult$
	display = $display$
	trigger = "
		$trigger$

		inline_script = {
			script = jobs/helper/tec_planet_not_exists_or_job_trigger
			trigger = tec_has_colonist_half_planet_swap
			negate = yes
		}

		owner = {
			inline_script = { script = tec_read_cache scope = country trigger = tec_is_dystopian_empire }
		}
	"
	default_job = $default_job$
	default_job = no
	default_trigger = "$default_trigger$"
	default_trigger = "always = no"
}

inline_script = {
	script = jobs/tec_job
	job = warden
	count = $count$
	mult = $mult$
	display = $display$
	trigger = "
		$trigger$

		planet = {
			has_modifier = penal_colony
		}
		owner = {
			inline_script = { script = tec_read_cache scope = country trigger = tec_uses_regular_empire_jobs }
		}
	"
	default_job = $default_job$
	default_job = no
	default_trigger = "$default_trigger$"
	default_trigger = "always = no"
}

inline_script = {
	script = mod_support/tec_inlines_include
	include_script = jobs/tec_colonist_colony_swap
	parameters = "
		count = $count$
		mult = $mult$
		display = $display$
		trigger = \"\\\"$trigger$\"\\\"
		default_job = $default_job$
		default_job = no
		default_trigger = \"\\\"$default_trigger$\"\\\"
		default_trigger = \"\\\"always = no\"\\\"
	"
}