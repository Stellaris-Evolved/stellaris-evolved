# jobs/tec_replicator.txt

# Regular
inline_script = {
	script = jobs/tec_job
	job = replicator
	count = $count$
	mult = $mult$
	display = $display$
	trigger = "
		exists = owner

		$trigger$

		owner = {
			is_machine_empire = yes
		}
	"
	default_job = $default_job$
	default_job = no
	default_trigger = "$default_trigger$"
	default_trigger = "always = no"
}

