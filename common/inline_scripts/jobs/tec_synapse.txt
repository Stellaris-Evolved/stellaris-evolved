# NOTE: This is not used in hive capital as hive type should already know its synapse type for performance

inline_script = { # Resonance
	script = jobs/tec_job
	trigger = "
		exists = owner

		$trigger$
		
		owner = {
			is_hive_empire = yes
			tec_is_cyberhive = no
			tec_is_biohive = no
		}
	"
	job = tec_resonance_drone
	count = $count$
	display = $display$
}

inline_script = { # Network
	script = jobs/tec_job
	trigger = "
		exists = owner

		$trigger$
		
		owner = {
			is_hive_empire = yes
			tec_is_cyberhive = yes
		}
	"
	job = tec_network_drone
	count = $count$
	display = $display$
}

inline_script = { # Synapse
	script = jobs/tec_job
	trigger = "
		exists = owner

		$trigger$
		
		owner = {
			is_hive_empire = yes
			tec_is_biohive = yes
		}
	"
	job = synapse_drone
	count = $count$
	display = $display$
}