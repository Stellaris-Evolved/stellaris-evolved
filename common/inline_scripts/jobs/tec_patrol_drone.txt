# jobs/tec_patrol_drone

# Maybe separate later between machines and hives

inline_script = { # Patrol Drone
	script = jobs/tec_job
	trigger = "
		exists = owner

		$trigger$
		
		owner = {
			is_gestalt = yes
			NOT = { inline_script = { script = tec_read_cache scope = country trigger = tec_has_patrol_drone_swap } }
		}
	"
	job = patrol_drone
	count = $count$
	mult = $mult$
	display = $display$
}


inline_script = {
	script = mod_support/tec_inlines_include
	include_script = jobs/tec_patrol_drone
	parameters = "
		count = $count$
		mult = $mult$
		display = $display$
		trigger = \"\\\"$trigger$\"\\\"
	"
}