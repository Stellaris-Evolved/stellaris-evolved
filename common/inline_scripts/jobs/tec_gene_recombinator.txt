# common\inline_scripts\jobs\tec_gene_recombinator.txt

inline_script = {
	script = jobs/tec_job
	trigger = "
		exists = owner

		$trigger$

		owner = {
			is_gestalt = no
		}
	"
	job = tec_gene_recombinator
	count = $count$
	mult = $mult$
	display = $display$
	default_job = $default_job$
	default_job = no
	default_trigger = "$default_trigger$"
	default_trigger = "always = no"
}

inline_script = {
	script = jobs/tec_job
	trigger = "
		exists = owner

		$trigger$

		owner = {
			is_gestalt = yes
		}
	"
	job = tec_gene_recombinator_drone
	count = $count$
	mult = $mult$
	display = $display$
	default_job = $default_job$
	default_job = no
	default_trigger = "$default_trigger$"
	default_trigger = "always = no"
}