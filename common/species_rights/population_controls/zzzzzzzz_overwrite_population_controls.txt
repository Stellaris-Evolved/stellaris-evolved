# common\species_rights\population_controls\zzzzzzzz_overwrite_population_controls.txt

population_control_yes = {
	can_reproduce = no

	pop_modifier = {
		pop_happiness = -0.1
	}

	allow = {
		hidden_trigger = { exists = from }
		from = {
			NOT = { has_policy_flag = population_controls_not_allowed }
		}
		if = {
			limit = {
				OR = {
					has_citizenship_type = { country = from type = citizenship_purge }
					has_citizenship_type = { country = from type = citizenship_purge_machine }
				}
			}
			hidden_trigger = { always = yes }
		}
		else = {
			custom_tooltip = {
				fail_text = same_species_disallowed
				NOT = { is_same_species = from }
			}
			#custom_tooltip = {	# This is funnier leaving it out - under new management!
			#	fail_text = is_broken_shackles_origin
			#	NOT = {
			#		from = {
			#			has_origin = origin_broken_shackles
			#		}
			#	}
			#}
			custom_tooltip = {
				fail_text = empire_xenophile
				from = {
					NOR = {
						has_ethic = ethic_xenophile
						has_ethic = ethic_fanatic_xenophile
					}
				}
			}
		}
	}
	ai_will_do = {
		factor = 2
		modifier = {
			factor = 0
			NOR = {
				has_citizenship_type = { country = from type = citizenship_purge_machine }
				has_citizenship_type = { country = from type = citizenship_purge }
			}
		}
	}
}

population_control_no = {
	potential = {
		always = yes
	}

	allow = {
		hidden_trigger = { exists = from }
		custom_tooltip = {
			NOR = {
				has_citizenship_type = {
					country = from 
					type = citizenship_purge_machine 
				}
				has_citizenship_type = {
					country = from 
					type = citizenship_purge 
				}
			}
			fail_text = is_undesirables_tooltip					#text = not_undesirables_tooltip
		}
		custom_tooltip = {
			fail_text = "HIVE_MIND_SPECIES_CITIZENS_ONLY"
			OR = {
				NOT = {
					from = {
						is_hive_empire = yes
					}
				}

				has_trait = trait_hive_mind

				AND = {	# robot exception
					has_trait = trait_mechanical
					from = {
						has_technology = tech_robotic_workers
						tec_is_biohive = no
					}
				}

				AND = {
					NOT = {
						has_trait = trait_hive_mind
					}
					from = {
						OR = {
							has_ethic = ethic_gestalt_empathy
							has_origin = origin_necrophage
						}
					}
				}
				
			}
		}
	}
	ai_will_do = {
		factor = 1
		modifier = {	# lets not get necrophages ais without pops
			factor = 100
			from = {
				has_origin = origin_necrophage
			}
		}
	}
}