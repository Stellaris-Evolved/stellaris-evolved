# common\agreement_term_values\zz_evolved_agreement_term_values.txt

subject_tec_satellite = {
	term = tec_satellite

	loyalty_change = 100

	owner_modifier = {
		divided_patrongage_max_subjects = 0.5
	}

	target_modifier = {
		monthly_loyalty = 3
	}

	potential = {
		agreement_preset = preset_tec_satellite
	}
	activate_effect = {
		if = {
			limit = {
				NOT = { has_agreement_flag = tec_init_satelite_lock }
			}

			set_agreement_flag = tec_init_satelite_lock
			if = {
				limit = {
					has_overlord_dlc = yes
				}
				set_agreement_preset = { # try to lock choices?
					preset = preset_tec_satellite
				}
			} else = {

				set_agreement_preset = { # try to lock choices?
					preset = preset_tec_satellite_no_overlords_dlc
				}
			}
		}
	}

	extra_term_text_overlord = subject_tec_satellite_term
}
