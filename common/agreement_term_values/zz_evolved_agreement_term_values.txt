subject_is_pompous = {
    term = subject_pompous

    triggered_loyalty_change = {
        trigger = {
            target = {
                tec_is_pompous_empire = yes
            }
        }
        loyalty_change = -25
        ai_acceptance = {
            subject = -125
        }
    }
}

subject_tec_satellite = {
    term = tec_satellite

    loyalty_change = 100

    owner_modifier = {
        divided_patrongage_max_subjects = 1
    }

    target_modifier = {
        monthly_loyalty = 4
    }

    potential = {
        agreement_preset = preset_tec_satellite
    }
    activate_effect = {
        if = {
            limit = {
                NOT = { has_agreement_flag = tec_init_satelite_lock }
            }

            set_agreement_flag = tec_init_satelite_lock
            set_agreement_preset = { # try to lock choices?
                preset = preset_tec_satellite
            }
        }
    }

    extra_term_text_overlord = subject_tec_satellite_term
}
