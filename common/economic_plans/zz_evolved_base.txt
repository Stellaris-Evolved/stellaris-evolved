# Suplementary evolved plans

basic_economy_plan = {
	subplan = {
		optional = yes
		set_name = "empire base living metal + nanites"
		potential = {
			always = yes
		}

		income = {
			sr_living_metal = @tec_economic_plan_exotic_resource_target
			nanites = @tec_economic_plan_exotic_resource_target
		}
	}

	subplan = {
		optional = yes
		scaling = yes
		set_name = "empire scaling living metal"
		potential = {
			has_monthly_income = { resource = sr_living_metal value < @tec_economic_plan_rare_resource_limit }
		}
		income = {
			sr_living_metal = @tec_economic_plan_scaling_rare_resource_target
		}
	}

	subplan = {
		optional = yes
		scaling = yes
		set_name = "empire scaling nanites"
		potential = {
			has_monthly_income = { resource = nanites value < @tec_economic_plan_rare_resource_limit }
		}
		income = {
			nanites = @tec_economic_plan_scaling_rare_resource_target
		}
	}


	subplan = {
		optional = yes
		set_name = "empire base zro"
		potential = {
			tec_has_any_psionic_ascension_finished = yes
		}

		income = {
			sr_zro = @tec_economic_plan_exotic_resource_target
		}
	}

	subplan = {
		optional = yes
		scaling = yes
		set_name = "empire scaling zro"
		potential = {
			tec_has_any_psionic_ascension_finished = yes
			has_monthly_income = { resource = sr_zro value < @tec_economic_plan_rare_resource_limit }
		}
		income = {
			sr_zro = @tec_economic_plan_scaling_rare_resource_target
		}
	}
}
