## scopes = { this = leader root = country prev = country }
alias[effect:tec_country_refragment_aiauth_node] = {
    FRAGMENT_EFFECT = <scripted_effect>
}

definitions = {
    ### some documentation
    ## scopes = { this = country root = country }
    ## definition_type = scripted_effect
    tec_country_refragment_aiauth_node
}