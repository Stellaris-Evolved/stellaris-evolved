building_archaeostudies_faculty = {
    base_buildtime = @b2_time
    ai_estimate_without_unemployment = yes

    category = research
    base_cap_amount = 1
    empire_limit = {
        base = 1
    }

    prerequisites = {
        "tech_archaeostudies"
    }

    potential = {
        exists = owner
        tec_planet_blocks_research_buildings = no
        tec_ai_destroy_archaeostudies_faculty = no
    }

    allow = {
        has_upgraded_capital = yes
        tec_ai_allow_artifacts_dependant_building = yes
    }

    resources = {
        category = planet_buildings
        inline_script = {
            script = buildings/resources/tec_resource_cost
            tier = 3
        }
        cost = {
            minor_artifacts = @b2_rare_cost
        }
    }

    triggered_desc = {
        text = building_archaeostudies_faculty_relic_effect
    }

    inline_script = {
        script = jobs/tec_archaeoengineers
        count = 3
        mult = 1
        display = with_desc
        trigger = ""
        default_job = archaeoengineers
    }

    inline_script = {
        script = jobs/tec_archaeoengineers
        count = 2
        mult = 1
        display = no_desc
        trigger = "
				owner = { has_ascension_perk = ap_archaeoengineers }
			"
    }

    inline_script = {	# Technocrat
        script = jobs/tec_archaeoengineers
        count = 1
        mult = planet.value:tec_script_add_per_ascension|BASE|1|ASCENSION_BASE|0|ASCENSION_MULT|@tec_per_planet_ascension_mult_normal|MULT|1|ROUND|yes|
        display = no_desc
        trigger = "
				owner = { has_ascension_perk = ap_archaeoengineers }
			"
    }

    country_modifier = {
        category_archaeostudies_research_speed_mult = 0.5
        country_resource_max_minor_artifacts_add = 1000
    }

    triggered_country_modifier = {
        potential = {
            exists = owner
            owner = {
                has_ascension_perk = ap_archaeoengineers
            }
        }
        country_resource_max_minor_artifacts_add = 2000
    }

    triggered_country_modifier = {
        potential = {
            exists = owner
            owner = {
                has_ascension_perk = ap_archaeoengineers
            }
        }
        country_resource_max_minor_artifacts_add = 250
        mult = planet.value:tec_script_add_per_ascension|BASE|1|ASCENSION_BASE|0|ASCENSION_MULT|@tec_per_planet_ascension_mult_very_high|MULT|1|ROUND|yes|
    }

    triggered_country_modifier = {
        potential = {
            planet = {
                tec_planet_generates_relics_for_jobs = yes
            }
        }
        category_archaeostudies_research_speed_mult = 0.5
    }

    triggered_country_modifier = {
        potential = {
            gpm_mod_active = yes
            planet = {
                is_gpm_precursor_tomb = yes  # gpm doubles the effect
            }
        }
        category_archaeostudies_research_speed_mult = 0.5
    }


    triggered_desc = {
        trigger = {
            exists = owner
            owner = {
                has_ascension_perk = ap_archaeoengineers
            }
        }
        text = tec_building_ascension_scaling
    }

    inline_script = {
        script = buildings/tec_aibait
        trigger = "
				tec_planet_generates_relics_for_jobs = yes
			"
    }
}