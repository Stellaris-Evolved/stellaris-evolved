# common\scripted_triggers\zz_ow_scripted_triggers_portrait_system.txt

#leader, pop or species scope (when called with species scope, from subscope can contain a country species related to)
has_cybernetization_stage_1 = {
	OR = {
		AND = {
			is_scope_type = leader
			tec_leader_is_rudimentary_cyborg = yes
		}
		AND = {
			OR = {
				is_scope_type = pop
				is_scope_type = species
			}
			tec_is_rudimentary_cyborg_species = yes
		}
	}
}

#leader, pop or species scope (when called with species scope, from subscope can contain a country species related to)
has_cybernetization_stage_2 = {
	OR = {
		AND = {
			is_scope_type = leader
			tec_is_cyborg_species = yes
		}
		AND = {
			OR = {
				is_scope_type = pop
				is_scope_type = species
			}
			tec_leader_is_cyborg = yes
		}
	}
}
