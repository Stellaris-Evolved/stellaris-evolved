# events\zz_evolved_ascension_perk_events.txt

namespace = tec_voidborn

country_event = {
    id = tec_voidborn.1
    hide_window = yes

    is_triggered_only = yes
    trigger = {
        has_ascension_perk = ap_voidborn
        exists = from.planet
    }

    immediate = {
        from.planet = {
            if = {
                limit = {
                    any_moon = {
                        is_planet_class = pc_habitat
                    }
                    root = {
                        NOT = {
                            has_technology = tech_habitat_2
                        }
                    }
                }
                root = {
                    add_research_option = tech_habitat_2
                }
            }
        }
    }
}